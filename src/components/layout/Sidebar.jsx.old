import {
  FaChevronDown,
  FaChevronUp,
  FaServer,
  FaTachometerAlt,
  FaCogs,
  FaBook,
  FaLock,
  FaUnlock,
} from "react-icons/fa";
import { useState } from "react";
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuLabel,
  DropdownMenuItem,
  DropdownMenuSeparator,
} from "@/components/ui/dropdown-menu";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";

export default function Sidebar({ user, dashboards = [], onLogout }) {
  const [expanded, setExpanded] = useState({
    Platform: true,
    Models: false,
    Docs: false,
  });

  const toggle = (section) =>
    setExpanded((prev) => ({ ...prev, [section]: !prev[section] }));

  return (
    <aside className="w-64 h-screen border-r border-border bg-background text-foreground flex flex-col justify-between p-4">
      {/* Top App/Org Info */}
      <div>

        <div className="flex items-center justify-between mb-4 px-1">
          <img src="/nsn1_md.png" alt="Logo" className="h-10 w-auto" />
        </div>

        {/* Platform Section */}
        <div>
          <p className="text-xs uppercase font-medium text-muted-foreground mb-1 px-2">
            Your Network
          </p>
          <div>
            <div
              className="flex items-center justify-between px-3 py-2 rounded-md cursor-pointer hover:bg-muted transition text-sm"
              onClick={() => toggle("Platform")}
            >
              <span className="flex items-center gap-2">
                <FaLock />
                Heirachy
              </span>
              {expanded.Platform ? <FaChevronUp /> : <FaChevronDown />}
            </div>
            {expanded.Platform && (
              <ul className="ml-8 mt-1 space-y-1 text-sm text-muted-foreground">
                <li className="cursor-pointer hover:text-foreground">Companys</li>
                <li className="cursor-pointer hover:text-foreground">Locations</li>
                <li className="cursor-pointer hover:text-foreground">Fleets</li>
                <li className="cursor-pointer hover:text-foreground">Assets</li>
                <li className="cursor-pointer hover:text-foreground">Variables</li>
                <li className="cursor-pointer hover:text-foreground">Hardware</li>
              </ul>
            )}
          </div>
        </div>

        {/* Models */}
        <div>
          <div
            className="flex items-center justify-between px-3 py-2 rounded-md cursor-pointer hover:bg-muted transition mt-3 text-sm"
            onClick={() => toggle("Models")}
          >
            <span className="flex items-center gap-2">
              <FaUnlock />
              Hardware
            </span>
            {expanded.Models ? <FaChevronUp /> : <FaChevronDown />}
          </div>
        </div>

        {/* Docs */}
        <div>
          <div
            className="flex items-center justify-between px-3 py-2 rounded-md cursor-pointer hover:bg-muted transition mt-3 text-sm"
            onClick={() => toggle("Docs")}
          >
            <span className="flex items-center gap-2">
              <FaBook />
              Documentation
            </span>
            {expanded.Docs ? <FaChevronUp /> : <FaChevronDown />}
          </div>
        </div>

        {/* Static Settings Item */}
        <div className="flex items-center gap-2 px-3 py-2 rounded-md hover:bg-muted cursor-pointer transition mt-3">
          <FaCogs />
          Settings
        </div>
      </div>

      {/* User Info + Dropdown */}
      <div className="border-t border-border pt-4 px-2">
        <DropdownMenu>
          <DropdownMenuTrigger className="w-full">
            <div className="flex items-center justify-between cursor-pointer hover:bg-muted p-2 rounded-md transition">
              <div className="flex items-center gap-2">
                <Avatar className="h-8 w-8">
                  <AvatarImage src={user?.picture} alt={user?.name} />
                  <AvatarFallback>{user?.name?.[0] || "U"}</AvatarFallback>
                </Avatar>
                <div className="flex flex-col text-left">
                  <p className="text-sm font-medium leading-none">{user?.name}</p>
                  <p className="text-xs text-muted-foreground">{user?.email}</p>
                </div>
              </div>
              <FaChevronDown className="text-muted-foreground" />
            </div>
          </DropdownMenuTrigger>
          <DropdownMenuContent className="w-full mt-1">
            <DropdownMenuLabel>My Account</DropdownMenuLabel>
            <DropdownMenuSeparator />
            <DropdownMenuItem onClick={() => alert("Profile clicked")}>
              Profile
            </DropdownMenuItem>
            <DropdownMenuItem onClick={() => alert("Settings clicked")}>
              Settings
            </DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem onClick={onLogout}>Logout</DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </aside>
  );
}

